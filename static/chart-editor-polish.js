/* Legacy chart polish compatibility shim.
 * Keep this file inert: DrawingManager remains the sole interaction owner.
 * The restored terminal is loaded by a dedicated Safari-safe loader.
 */
import("/static/chart-terminal-loader.js");
