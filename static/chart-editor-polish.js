/* Retired compatibility shim.
 *
 * Drawing-tool state, Pointer Events, mobile/desktop tool selection, outside
 * tap handling, handle visibility and fullscreen editor behavior now live in
 * chart-mobile-interactions.js. This file intentionally has no runtime side
 * effects and remains only because index.html still references the historical
 * asset name; it can be removed in a later asset-cleanup change.
 */
